# Example environment variables for blackjack-crew
# Prefer CREWAI-specific env vars so the Crew runtime uses the correct provider
CREWAI_LLM_PROVIDER=openai
CREWAI_MODEL=gpt-4o-mini
OPENAI_API_KEY=OPENAI_API_KEY_HERE
AUTO_ACCEPT_HUMAN=false # Set to true to auto-accept human prompts so the run is non-interactive



